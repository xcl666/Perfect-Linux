sudo cp sources.list /etc/apt/

sudo apt install vim sudo tmux git wget zsh fcitx-googlepinyin w3m shadowsocks-libev command-not-found python3-pip fortune

Now imagine that: I get a server hardware.
1, I install the Debian GNU/Linux on it.
	How to install the os?
	a, Use the usb to book.
	b, It is best without X now.
2, I will install software on it to make the environment fit me well.
	python3-pip
	fcitx-googlepinyin
	aptitude
	axel
	sources.list
	mutt
	fortune
	nfs-common
	vim
	sudo
	tmux
	git
	wget
	zsh 
	oh-my-zsh (sh -c "$(wget https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh -O -)")
	fcitx
	w3m
	shadowsocks-libev
	command-not-found
	cheat
	ftp
	lftp
	i3
	rofi
	chromium
	tree
	exuberant-ctags
	rdesktop
	unar
3, Laptop
	volumeicon
	mate-power-manager
	iw
	wpasupplicant
